# 一定要注意Link 数量，避免出现内存溢出，

 shouldLink 逻辑未正确可能导致 Link 过度创建，产生卡度

# 需要替换的 api

```js
// 10.8ms 判断是否 叶子节点，包括 在 scope 中直接 get的 computed 以及 effect 本身
const isLeaf = !emitStart || emitStart.downstream === node.scope;

// 
```

